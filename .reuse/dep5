Format: https://www.debian.org/doc/packaging-manuals/copyright-format/1.0/
Upstream-Name: Hifz Companion
Source: https://github.com/hifz-companion/app

# The repository's documentation, planning, design-system, research, skills,
# clickable demo, and developer tooling scripts. These are project content
# authored by the copyright holder and are GPL-3.0-or-later, the same license as
# the app code. Future package source (packages/, app/) carries its own per-file
# SPDX header instead — it is deliberately NOT bulk-covered here, so the
# per-file licensing discipline the REUSE gate enforces stays in force.
Files:
 .gitignore
 CLAUDE.md
 LICENSE
 *.js
 .claude/*
 docs/*
 epics/*
 research/*
 demo/*
 design-explorations/*
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later

# Binary and font assets that cannot carry an inline SPDX header. UI assets in
# THIS repo (app icons, UI fonts) are GPL-3.0-or-later. The muṣḥaf glyph fonts
# and the Quran text are NOT in this repo — they live in the Quran-data repo
# under their own terms (KFGQPC font terms; Tanzil text verbatim) and are never
# relicensed here. See LICENSING.md.
Files: *.png *.ico *.jpg *.jpeg *.webp *.gif *.svg *.ttf *.otf *.woff *.woff2
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later

# The bundled Vazirmatn UI font (rastikerdar/vazirmatn) is third-party under the
# SIL Open Font License 1.1 — NOT first-party and NOT relicensed. This stanza
# overrides the generic *.ttf rule above for these files (dep5 last-match-wins),
# so the font keeps its own license and the upstream authors' copyright.
Files: app/assets/fonts/Vazirmatn-*.ttf
Copyright: 2015 The Vazirmatn Project Authors (https://github.com/rastikerdar/vazirmatn)
License: OFL-1.1

# The app shell's generated platform glue (Android/iOS) and Flutter metadata —
# boilerplate (gradle, xml, plist, swift, kotlin, xcconfig, asset catalogs) that
# cannot carry an inline SPDX header cleanly. GPL-3.0-or-later, the same as the
# app code. The Gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) is
# gitignored by Flutter's template and is not committed, so no third-party
# Apache-2.0 attribution is needed here.
Files:
 app/.gitignore
 app/.metadata
 app/android/*
 app/ios/*
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later

# Generated/structured files that cannot carry an inline SPDX comment: the ARB
# translation sources (JSON, no comment syntax) and the single committed
# workspace pubspec.lock (the audit map). GPL-3.0-or-later, the same as the code.
Files:
 packages/l10n/lib/src/arb/*.arb
 pubspec.lock
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later

# Placeholder that keeps the (gitignored) core-asset font directory present on a
# fresh checkout so the pubspec asset declaration resolves; carries no content.
# The real KFGQPC fonts are never committed (see the README beside it).
Files: app/assets/quran/fonts/.gitkeep
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later

# Code-generated Dart (drift database glue, and any future *.drift.dart /
# *.freezed.dart). These regenerate from hand-written schema/table classes that
# each carry their own per-file SPDX header, and are overwritten on every
# `build_runner build`, so a maintained inline header is impossible. They are
# first-party derived work under the same license; the per-file discipline stays
# in force for the authored sources that drive them.
Files: *.g.dart *.drift.dart *.freezed.dart
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later

# drift_dev-generated migration artifacts (`schema dump` → `schema generate` →
# `schema steps`): the committed DO-NOT-MODIFY JSON snapshots of past schema
# versions, the generated SchemaVerifier fixtures, and the stepByStep helper.
# Regenerated by the drift_dev CLI, they cannot carry a maintained inline
# header; first-party derived work under the same license, driven by the
# per-file-licensed table classes.
Files:
 packages/data/drift_schemas/*.json
 packages/data/lib/src/db/schema_versions.dart
 packages/data/test/migration/schema/*.dart
Copyright: 2026 Zakaria Fatahi and Hifz Companion contributors
License: GPL-3.0-or-later
