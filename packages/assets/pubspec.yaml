# SPDX-FileCopyrightText: 2026 Zakaria Fatahi and Hifz Companion contributors
# SPDX-License-Identifier: GPL-3.0-or-later

# Resolution stub — the network-quarantine package: the ONLY package allowed to
# declare http/crypto (the one-time, fail-closed asset-pack downloader). Its full
# manifest (http, crypto, path), barrel, and mocked-client tests are authored in
# E01-T04 / E05.
name: assets
description: Offline asset-pack downloader + SHA-256 verifier for Hifz Companion (stub).
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
